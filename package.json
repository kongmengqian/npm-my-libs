{
  "name": "kongmq-npmlibs",
  "version": "0.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:demo": "lerna run start --scope=kongmq-demo --stream",
    "dev:demo": "lerna run dev --scope=kongmq-demo --stream",
    "build:demo": "lerna run build --scope=kongmq-demo --stream"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
